spring:
  application:
    name: evotrace-server
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/evotrace
    username: evotrace
    password: evotrace
  flyway:
    enabled: true
    locations: classpath:db/migration
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: latest
      properties:
        spring.json.trusted.packages: "io.evotrace.protocol,io.evotrace.server"
        spring.json.use.type.headers: false
        spring.json.value.default.type: io.evotrace.protocol.envelope.Envelope
  ai:
    openai:
      base-url: ${AI_BASE_URL:https://api.openai.com}
      api-key: ${AI_API_KEY:sk-placeholder}
      chat:
        options:
          model: ${AI_CHAT_MODEL:gpt-4o-mini}
          temperature: 0.2

evotrace:
  server-url: ${EVOTRACE_SERVER_URL:}     # 环境自检回环地址，空则从请求推导
  ingestion:
    max-inline-diff-bytes: 262144
    signature-header: X-EvoTrace-Signature
  test-executor:
    request-timeout-ms: 8000
    insecure-tls: ${EVOTRACE_TEST_INSECURE_TLS:false}
    max-response-chars: 2000
    max-steps: 20
  blob:
    dir: ${EVOTRACE_BLOB_DIR:./data/blobs}
  gitlab:
    # Personal/Project Access Token with read_api (or read_repository)
    token: ${GITLAB_TOKEN:}
    # Optional; default derives from webhook project.web_url host
    base-url: ${GITLAB_BASE_URL:}
    fetch-diff: true
    max-diff-chars-per-file: 200000
  partition:
    months-ahead: 2
    maintenance-cron: "0 30 0 1 * *"
  snapshot:
    full-baseline-interval: 20
    cron: "0 0 3 * * *"
  code-review:
    auto-enabled: false
    max-diff-chars: 12000
  ai:
    privacy-default: STRUCTURE_ONLY   # FULL_CODE / STRUCTURE_ONLY / LOCAL_ONLY
    summary-model: ${AI_CHAT_MODEL:gpt-4o-mini}
    release-notes-model: ${AI_CHAT_MODEL:gpt-4o-mini}
  clickhouse:
    # V2.5 分析库：事件流/分析查询。未配置时优雅降级（不启用，仍走 PostgreSQL）。
    enabled: ${CLICKHOUSE_ENABLED:false}
    url: ${CLICKHOUSE_URL:jdbc:clickhouse://localhost:8123/evotrace}
    username: ${CLICKHOUSE_USER:default}
    password: ${CLICKHOUSE_PASSWORD:}
    # 事件双写：true 时摄入管道同时写 ClickHouse（双写灰度过渡）
    dual-write: ${CLICKHOUSE_DUAL_WRITE:false}
    backfill:
      enabled: ${CLICKHOUSE_BACKFILL_ENABLED:false}
      cron: "0 30 4 * * *"
      batch-size: 5000

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      app: evotrace-server

server:
  port: 8080
