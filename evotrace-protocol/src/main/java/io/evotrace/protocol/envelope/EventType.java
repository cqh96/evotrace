package io.evotrace.protocol.envelope;

public enum EventType {
    CODE_COMMIT,
    MR_MERGED,
    RELEASE_TAG,
    INVENTORY_REPORT,
    CONFIG_CHANGE,
    DDL_CHANGE,
    ITERATION_SYNC,
    DEPLOY_RECORD
}
